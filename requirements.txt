# --- Core MCP + agent runtime ---
fastmcp>=2.0            # FastMCP: MCP server/client framework (lecturer-recommended)
mcp>=1.2                # Model Context Protocol SDK (transport)

# --- LLM providers (modular; only the selected one is used at runtime) ---
ollama>=0.3             # local LLM client (default, free, unlimited)
anthropic>=0.40         # cheap paid fallback (Haiku 4.5)
google-generativeai>=0.8  # Gemini option

# --- Config, reporting, visualization ---
pyyaml>=6.0
python-dotenv>=1.0
numpy>=1.26              # Q-table
matplotlib>=3.8          # board renderer + Q-table heatmap

# --- Email (Gmail API) ---
google-api-python-client>=2.0
google-auth-httplib2>=0.2
google-auth-oauthlib>=1.2

# --- Quality tooling (rubric: Quality Standards) ---
pytest>=8.0
ruff>=0.6
black>=24.0
pre-commit>=3.8
